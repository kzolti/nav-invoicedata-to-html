const { translations: hu } = require("./tr/hu");
const { translations: en } = require("./tr/en");
const { translations: cn } = require("./tr/cn");

const translations:any = {
  hu: hu,
  en: en,
  cn: cn
};

export default translations;
